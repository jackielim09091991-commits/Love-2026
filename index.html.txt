<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>2026 Valentine's Memory</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <style>
        :root {
            --accent: #ffb3b3;
            --bg: #0a0a0a;
        }

        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; }

        body, html {
            margin: 0; padding: 0; width: 100%; height: 100%;
            background-color: var(--bg);
            color: white; font-family: -apple-system, "Helvetica Neue", sans-serif;
            overflow: hidden;
        }

        /* 音乐唱片按钮 */
        #music-btn {
            position: fixed; top: 25px; right: 25px; z-index: 100;
            width: 44px; height: 44px;
            background: rgba(255,255,255,0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.2);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            cursor: pointer;
        }

        .rotating { animation: spin 4s linear infinite; }
        @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

        /* 相册容器 */
        .swiper { width: 100%; height: 100vh; }

        .swiper-slide {
            display: flex; flex-direction: column;
            align-items: center; justify-content: center;
            padding: 20px;
        }

        /* 拍立得卡片风格 */
        .polaroid-card {
            background: #fff;
            padding: 12px 12px 40px 12px;
            box-shadow: 0 20px 50px rgba(0,0,0,0.5);
            transform: rotate(-2deg);
            max-width: 90%;
            transition: transform 0.5s ease;
        }

        .swiper-slide-active .polaroid-card {
            transform: rotate(0deg) scale(1.05);
        }

        .polaroid-card img {
            width: 100%;
            height: auto;
            max-height: 60vh;
            object-fit: cover;
            display: block;
        }

        /* 文案展示 */
        .story-content {
            margin-top: 40px;
            text-align: center;
            max-width: 80%;
        }

        .story-content h2 {
            font-weight: 200;
            letter-spacing: 3px;
            font-size: 1.1rem;
            color: var(--accent);
            margin: 0;
        }

        .story-content p {
            font-size: 0.75rem;
            color: #666;
            margin-top: 10px;
            letter-spacing: 1px;
        }

        /* 底部指示器 */
        .footer-info {
            position: absolute; bottom: 30px;
            font-size: 0.65rem; color: #333;
            letter-spacing: 2px;
        }
    </style>
</head>
<body>

    <div id="music-btn">🎵</div>
    
    <audio id="bgm" src="bgm.mp3" loop></audio>

    <div class="swiper">
        <div class="swiper-wrapper">
            <div class="swiper-slide">
                <div class="polaroid-card"><img src="https://i.ibb.co/9mFF3cJF/Whats-App-Image-2026-02-13-at-11-16-40-PM-1.jpg"></div>
                <div class="story-content"><h2>Page 01</h2><p>Our journey begins here.</p></div>
            </div>
            <div class="swiper-slide">
                <div class="polaroid-card"><img src="https://i.ibb.co/cShRN19d/Whats-App-Image-2026-02-13-at-11-16-40-PM-2.jpg"></div>
                <div class="story-content"><h2>Page 02</h2><p>Every moment with you is a gift.</p></div>
            </div>
            <div class="swiper-slide">
                <div class="polaroid-card"><img src="https://i.ibb.co/4Z11Tc0P/Whats-App-Image-2026-02-13-at-11-16-40-PM-3.jpg"></div>
                <div class="story-content"><h2>Page 03</h2><p>Beautiful in White...</p></div>
            </div>
            <div class="swiper-slide">
                <div class="polaroid-card"><img src="https://i.ibb.co/7x4Jmhkq/Whats-App-Image-2026-02-13-at-11-16-40-PM.jpg"></div>
                <div class="story-content"><h2>The Best is Yet to Come</h2><p>Happy Valentine's Day 2026</p></div>
            </div>
        </div>
        <div class="footer-info">SWIPE TO EXPLORE</div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script>
        // 初始化
        const swiper = new Swiper('.swiper', {
            effect: 'cards', // 使用层叠卡片效果，更有“翻相册”的感觉
            grabCursor: true,
            loop: true,
        });

        const audio = document.getElementById('bgm');
        const btn = document.getElementById('music-btn');

        function toggleMusic() {
            if (audio.paused) {
                audio.play();
                btn.classList.add('rotating');
            } else {
                audio.pause();
                btn.classList.remove('rotating');
            }
        }

        btn.onclick = toggleMusic;
        // 第一次交互自动触发音乐
        document.body.onclick = function() {
            if (audio.paused) toggleMusic();
        }
    </script>
</body>
</html>